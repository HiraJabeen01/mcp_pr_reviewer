[project]
name = "asana-mcp-server"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv",
  "pydantic",
  "fastmcp",
  "mcp",
  "loguru",
  "pytest-asyncio",
  "pytest",
  "requests",
  "opik"
  ]
